name: dockly_api
description: Dockly API istemcisi — Dio + DTO'lar + Problem eşleme (docs/26 §6). Saf Dart.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.7.0
  dockly_core:
    path: ../dockly_core

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
