name: dockly_core
description: Dockly ortak çekirdek — saf Dart, Flutter'sız (docs/26 §1-2 domain katmanı).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
